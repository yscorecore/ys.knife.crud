# pnpm 配置
shamefully-hoist=false
strict-peer-dependencies=false
auto-install-peers=true
# 本仓库默认使用 hoisted node-linker，避免沙箱拦截 pnpm 的 virtual-store link 重写。
node-linker=hoisted

# 发布到 npm 时使用公共访问权限（scoped 包默认为 restricted）
# 发布前执行: npm login
# registry=https://registry.npmjs.org/
